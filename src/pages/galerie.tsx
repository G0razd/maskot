import { PageProps } from "gatsby"
import { Layout } from "@components"
import React from "react"

const Galerie: React.FC<PageProps> = () => {
  return (
    <Layout>
      <h1>Galerie</h1>
    </Layout>
  )
}

export default Galerie
